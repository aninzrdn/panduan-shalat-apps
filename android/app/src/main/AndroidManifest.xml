<manifest xmlns:android="http://schemas.android.com/apk/res/android" 
    package="com.example.tutorial_sholat">
    <!-- Izin untuk mengakses lokasi -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Izin untuk mengakses jaringan (untuk paket http) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Izin untuk mengakses jaringan (opsional) -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Izin untuk mengakses jaringan (opsional) -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Izin untuk mengakses sumber daya jaringan (opsional) -->
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <!-- Izin untuk mengakses sumber daya jaringan (opsional) -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- Izin untuk mengakses jaringan (opsional) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Izin untuk mengakses pengaturan jaringan (opsional) -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />

    <!-- Izin untuk mengakses jaringan (opsional) -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Izin untuk mengakses jaringan (opsional) -->
    <uses-permission android:name="android.permission.BLUETOOTH" />

    <!-- Izin untuk mengakses jaringan (opsional) -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <!-- Izin untuk mengakses jaringan (opsional) -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Izin untuk mengakses jaringan (opsional) -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />

    <!-- Izin untuk mengakses jaringan (opsional) -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

    <!-- Izin untuk mengakses jaringan (opsional) -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Izin untuk mengakses koneksi internet (opsional) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Izin untuk mengakses jaringan seluler (opsional) -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Izin untuk mengakses Wi-Fi (opsional) -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Izin untuk mengatur Wi-Fi (opsional) -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- Izin untuk mengontrol getaran (opsional) -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Izin untuk mengakses jaringan Bluetooth (opsional) -->
    <uses-permission android:name="android.permission.BLUETOOTH" />

    <!-- Izin untuk mengakses administrasi Bluetooth (opsional) -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <!-- Izin untuk mengakses koneksi Bluetooth (opsional) -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Izin untuk memindai perangkat Bluetooth (opsional) -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />

    <!-- Izin untuk beriklan melalui Bluetooth (opsional) -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

    <!-- Izin untuk berkomunikasi dengan perangkat Bluetooth (opsional) -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Izin untuk mengakses pengaturan (opsional) -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />

    <!-- Izin untuk mengakses lokasi GPS (opsional) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Izin untuk mengakses lokasi jaringan (opsional) -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Izin untuk menulis ke penyimpanan eksternal (opsional) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- Izin untuk membaca dari penyimpanan eksternal (opsional) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- Izin untuk mengambil foto dengan kamera (opsional) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Izin untuk merekam audio (opsional) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Izin untuk mengakses mikrofon (opsional) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Izin untuk mengakses kontak (opsional) -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    
    <!-- Izin untuk menulis ke kontak (opsional) -->
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    
    <!-- Izin untuk membaca pesan SMS (opsional) -->
    <uses-permission android:name="android.permission.READ_SMS" />

    <!-- Izin untuk mengirim SMS (opsional) -->
    <uses-permission android:name="android.permission.SEND_SMS" />

    <!-- Izin untuk menerima SMS (opsional) -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <!-- Izin untuk mengakses panggilan telepon (opsional) -->
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!-- Izin untuk merekam panggilan (opsional) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Izin untuk mengakses penyimpanan internal (opsional) -->
    <uses-permission android:name="android.permission.WRITE_INTERNAL_STORAGE" />

    <!-- Izin untuk mengakses perangkat kamera (opsional) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Izin untuk membaca pesan MMS (opsional) -->
    <uses-permission android:name="android.permission.READ_MMS" />
    
    <!-- Izin untuk mengirim pesan MMS (opsional) -->
    <uses-permission android:name="android.permission.SEND_MMS" />
    <application
        android:label="Panduan Sholat"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
